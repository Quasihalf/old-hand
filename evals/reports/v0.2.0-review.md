# old-hand v0.2.0 Independent Release Review

Review date: 2026-07-11 (Asia/Shanghai)  
Reviewer: fresh Codex CLI 0.144.1 session using `gpt-5.6-terra`

## Result: PASS

The pre-release blockers were independently rechecked:

- GitHub state was verified with live `gh` and `git` queries.
- The remote marketplace installation and all three skill bundles were checked.
- No additional release-blocking issue was found.

## Blocking Findings

None.

The `v0.2.0` tag and GitHub Release are post-review publication steps, not
pre-release review blockers.

## Behavior And Trigger Quality

The skill routes project direction, dependency decisions, debugging, review,
and normal implementation while explicitly excluding purely mechanical work.
See [SKILL.md](../../skills/old-hand/SKILL.md).

The trigger fixtures include English and Chinese positive cases plus near-miss
negative cases for renaming, formatting, translation, general knowledge,
factual file reads, and mechanical Git inspection. See
[trigger-cases.json](../trigger-cases.json).

The research and security boundaries treat external content as untrusted and
forbid executing downloaded code or installing dependencies without an
independent need, safety, license, and fit review. See
[research-protocol.md](../../skills/old-hand/references/research-protocol.md) and
[SECURITY.md](../../SECURITY.md).

## Verification Evidence

- `gh repo view 2731350936/old-hand` reported a public repository with `main`
  as the default branch.
- Repository topics were `ai-coding`, `code-review`, `codex`, `codex-skill`,
  `debugging`, `open-source-research`, and `software-engineering`.
- GitHub private vulnerability reporting returned `{"enabled":true}`.
- Local `HEAD`, `origin/main`, remote `HEAD`, and the marketplace revision were
  all `7c34bedda1d893914a01b9654a8e588ed99b31bb` at review time.
- The initial GitHub Actions `Validate` run for that commit completed with
  `success`: [run 29113848661](https://github.com/2731350936/old-hand/actions/runs/29113848661).
- `codex plugin list` reported `old-hand@old-hand` as installed, enabled, and
  version `0.2.0`.
- The marketplace install record used
  `https://github.com/2731350936/old-hand.git`, ref `main`, and revision
  `7c34bedda1d893914a01b9654a8e588ed99b31bb`.
- Recursive diffs found no differences among the canonical
  `skills/old-hand/` bundle, the marketplace-installed bundle, and the local
  `~/.codex/skills/old-hand` bundle. All five file hashes also matched.
- `python3 -m unittest discover -s tests -v` passed all 16 tests locally and
  against the marketplace snapshot.
- `python3 scripts/validate.py .` passed locally and against the marketplace
  snapshot.
- The official plugin validator and skill validator passed in the release
  preparation session.
- JSON manifests and fixtures parsed successfully, and `git diff --check`
  passed.

## Evidence Boundaries

- Trigger fixtures are design and regression checks, not product-level
  instruction-injection traces.
- This review does not claim statistical trigger rates, cross-model or
  cross-platform performance, or behavior on future Codex versions.
- The qualitative baseline is a small evidence set, not a randomized A/B
  benchmark. See [baseline.md](../../benchmarks/baseline.md).
- Only the checks listed above are claimed.
- The marketplace workspace contains an installer-generated untracked
  `.codex-marketplace-install.json`; it records provenance and is not a
  canonical repository change.

## Release Recommendation

Commit this report, rerun the full release verification on the resulting
commit, create and push the `v0.2.0` tag from that verified commit, and publish
the GitHub Release.
